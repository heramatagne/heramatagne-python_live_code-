# Function to reverse a string
word = "python"

# using split('') function to split the string
print(word.split(' '))

#use join(reversed (variable)) function to reverse the string.
print(''.join(reversed(word)))